# =============================================================================
# 考研 AI 导学平台 · 后端依赖
#
# 安装:  pip install -r requirements.txt
# 说明:  只列后端真正需要的包, 版本用 ">=最低,<下一个大版本" 约束,
#        保证在任何机器上都能装成功(不绑定本机路径/conda 环境)。
#
# 曾经的问题: 本文件原先是从本机 conda 环境整体导出的 121 行清单,
# 混入了 tensorflow / torch / xgboost / matplotlib / conda 等无关包,
# 还包含 20 多个 "@ file:///C:/b/abs_..." 本机路径依赖, 换机器必然安装失败。
# =============================================================================

# ---------- Web 框架 ----------
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
python-multipart>=0.0.9          # 表单 / 文件上传

# ---------- 数据校验与配置 ----------
pydantic>=2.5,<3.0
python-dotenv>=1.0,<2.0          # 读取 .env

# ---------- 数据库 (MySQL) ----------
SQLAlchemy>=2.0,<3.0
PyMySQL>=1.1,<2.0
cryptography>=42.0               # MySQL 8 caching_sha2_password 认证需要
alembic>=1.13,<2.0               # 数据库迁移(逐步替换 create_all)

# ---------- 缓存 / 会话 ----------
redis>=5.0,<7.0

# ---------- 对象存储 (多模态文件) ----------
minio>=7.2,<8.0

# ---------- 认证 ----------
argon2-cffi>=23.1,<26.0          # 密码哈希(比 bcrypt 更省心, 无 72 字节限制)
PyJWT>=2.8,<3.0                  # 签发/校验 access token

# ---------- 实时通信 ----------
websockets>=12.0                 # WebSocket 支持(uvicorn[standard] 已带, 显式声明更清晰)

# ---------- 开发/自测(可选, 不装也能跑服务) ----------
# httpx>=0.27                    # 写接口测试时需要
# pytest>=8.0                    # 单元测试
